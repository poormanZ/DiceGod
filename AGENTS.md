# DiceGod — 개발 규칙

## 현재 기준

- Godot 4.7.2 / GDScript / 2D / GL Compatibility
- Web 배포 우선
- 1280×720 기준 반응형 UI
- 기본 주사위 6개 × 6면 행동 심볼
- 전투 전 빌드 선택 없음
- 코드 변경과 관련 문서를 같은 작업에서 동기화

## 핵심 심볼

⚔️ 검 / 🏹 활 / 🔮 지팡이 / 🗡️ 표창 / 🛡️ 방패 / ❤️ 힐

내부 ID는 식별자이며 공격력이나 회복량 자체가 아닙니다.

## 코드 원칙

1. 게임 규칙은 `docs/`의 현재 사양을 기준으로 합니다.
2. GDScript의 타입을 가능한 한 명시해 `Cannot infer` 계열 오류를 예방합니다.
3. 주사위 데이터는 `DiceData`, 전투 중 상태는 `DiceRuntimeState`, 굴림 규칙은 `DiceRoller`가 담당합니다.
4. 전투 화면은 입력/연출과 전투 규칙을 분리합니다.
5. 정적 콘텐츠는 Godot Resource를 우선 사용합니다.
6. 동일 규칙을 여러 스크립트에 중복 하드코딩하지 않습니다.
7. 요청 범위를 벗어난 대규모 게임 규칙 변경은 하지 않습니다.
8. 기능 변경 시 관련 설계 문서를 즉시 갱신합니다.
9. Web Export와 headless 검증이 깨지지 않도록 유지합니다.
10. 사용하지 않는 구형 빌드/숫자 주사위 개념을 새 코드에 다시 도입하지 않습니다.

## 현재 전투 루프

전투 시작 → 6개 굴림 → 잠금 → 1회 리롤 → 확정 → 공격/보호막/치료 적용 → 적 행동 → 다음 턴

## 작업 완료 기준

- 변경 파일과 책임을 확인합니다.
- 깨진 참조와 명백한 타입 오류를 확인합니다.
- GitHub Actions Web Export 검증 결과를 확인합니다.
- 현재 구현과 문서가 일치하는지 확인합니다.
